"""内核提取的差分等价基线。

`pinkpaw_core3_baseline.py` 是 `agent/custom/action/pinkpaw/pinkpaw_core3.py`
在内核提取**之前**的完整快照（对应 commit 02fd3a8 的版本）。

用途：`tools/verify_combat.py` 的第 11 组「内核等价性」用它做原实现 vs
新内核实现的逐项比对，断言阈值、打分、布尔判定完全一致。

约定：

- **不要删除**这个快照。它是长期回归基线，不是临时产物。
- **不要跟着 core3 一起改**。它的价值就在于冻结提取前的行为；
  如果哪天真的需要调整内核阈值，应当先让等价测试失败，再由人明确判断
  该差异是否为预期，然后连同基线一起更新并在 PR 里说明原因。
- 它不参与运行时导入，agent 不会加载它。

`baseline_loader.py` 负责把这个快照以「剥离 maa/ctypes 依赖」的方式导入，
只取其中的纯图像判定逻辑。
